import styled from '@emotion/styled';

export const Canvas = styled.canvas`
  position: absolute;
  top: 85px;
  left: 0;
  width: 100%;
  height: 100%;
  perspective: 800px;
  pointer-events: none;
`;
